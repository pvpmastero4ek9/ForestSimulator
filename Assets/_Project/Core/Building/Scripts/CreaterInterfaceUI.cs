using UnityEngine;
//using Ui.Building;

namespace Core.Building
{
    public class CreaterInterfaceUI : MonoBehaviour
    {
        //[SerializeField] private BuildingUI buildingUIPrefab;
        [SerializeField] private Transform uiRoot;
        [SerializeField] private BuildingContainerForUI container;

        //public void ShowInterface()
        //{
            //BuildingUI ui = Instantiate(buildingUIPrefab, uiRoot);
            //ui.Initialize(container);
        //}
    }
}
